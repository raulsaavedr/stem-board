name: Prepare Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: Semver bump kind.
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
      base:
        description: Branch to branch from and target — dev for normal releases, main for hotfixes.
        required: false
        type: choice
        default: dev
        options:
          - dev
          - main

concurrency:
  group: prepare-release
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  actions: write

env:
  CARGO_TERM_COLOR: always

jobs:
  prepare:
    name: prepare release
    runs-on: ubuntu-latest
    outputs:
      target_version: ${{ steps.plan.outputs.target }}
      pr_url: ${{ steps.pr.outputs.pr_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          # The release branch is cut from the selected base: dev for normal
          # releases, main only for hotfixes. The branch stays serialized by the
          # concurrency group above regardless of base.
          ref: ${{ inputs.base }}
          fetch-depth: 0
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Plan release bump
        id: plan
        shell: bash
        env:
          BUMP: ${{ inputs.bump }}
        run: |
          set -euo pipefail
          plan="$(python3 scripts/prepare-release.py --repo . plan --bump "$BUMP")"
          target="$(python3 -c 'import json,sys; print(json.loads(sys.argv[1])["target"])' "$plan")"
          current="$(python3 -c 'import json,sys; print(json.loads(sys.argv[1])["current"])' "$plan")"
          branch="release/v$target"
          echo "current=$current" >> "$GITHUB_OUTPUT"
          echo "target=$target" >> "$GITHUB_OUTPUT"
          echo "branch=$branch" >> "$GITHUB_OUTPUT"
          echo "Preparing release bump: $current -> $target"

      - name: Create release branch
        shell: bash
        env:
          BRANCH: ${{ steps.plan.outputs.branch }}
        run: git checkout -B "$BRANCH"

      - name: Apply release files
        shell: bash
        env:
          TARGET: ${{ steps.plan.outputs.target }}
          REPO_URL: https://github.com/${{ github.repository }}
        run: |
          set -euo pipefail
          python3 scripts/prepare-release.py --repo . apply \
            --target-version "$TARGET" \
            --date "$(date -u +%F)" \
            --repo-url "$REPO_URL"
          python3 scripts/prepare-release.py --repo . verify --repo-url "$REPO_URL"

      - name: fmt
        run: cargo fmt --all --check

      - name: clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: test Rust
        run: cargo test --workspace --all-features

      - name: Commit and push release branch
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET: ${{ steps.plan.outputs.target }}
          BRANCH: ${{ steps.plan.outputs.branch }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if [ -n "$(git status --porcelain)" ]; then
            # Stage exactly what the tool writes. This list used to be repeated
            # here, and when `apply` grew the install-ref repin the extra files
            # were rewritten on the runner and then dropped at `git add`, so
            # v0.9.1 was cut with stale `--ref v0.9.0` pins.
            python3 scripts/prepare-release.py --repo . files \
              | tr '\n' '\0' | xargs -0 --no-run-if-empty git add --
            git commit -m "chore(release): v$TARGET"
          else
            echo "prepare-release.py made no file changes"
          fi

          remote_url="https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          remote_sha="$(git ls-remote --heads "$remote_url" "$BRANCH" | awk '{print $1}')"
          if [ -n "$remote_sha" ]; then
            git push --force-with-lease="refs/heads/$BRANCH:$remote_sha" "$remote_url" HEAD:"$BRANCH"
          else
            git push "$remote_url" HEAD:"$BRANCH"
          fi

      - name: Create or update PR
        id: pr
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET: ${{ steps.plan.outputs.target }}
          BRANCH: ${{ steps.plan.outputs.branch }}
        run: |
          set -euo pipefail
          title="chore(release): v$TARGET"
          body="$(printf '%s\n' \
            "Prepare release branch for v$TARGET (base \`${{ inputs.base }}\`)." \
            "" \
            "- release files synced by scripts/prepare-release.py" \
            "- CI is dispatched explicitly from this workflow" \
            "- base dev: a maintainer merges the Promote workflow's dev -> main PR, then Release tags the green commit" \
            "- base main (hotfix): Release tags the green main commit directly")"

          pr_json="$(gh pr list --head "$BRANCH" --state open --json number,url)"
          if [ "$(printf '%s' "$pr_json" | python3 -c 'import json,sys; print(len(json.load(sys.stdin)))')" -gt 0 ]; then
            pr_number="$(printf '%s' "$pr_json" | python3 -c 'import json,sys; print(json.load(sys.stdin)[0]["number"])')"
            pr_url="$(printf '%s' "$pr_json" | python3 -c 'import json,sys; print(json.load(sys.stdin)[0]["url"])')"
            # Retarget on reruns: an earlier run may have used another base.
            gh pr edit "$pr_number" --title "$title" --body "$body" --base "${{ inputs.base }}"
          else
            pr_url="$(gh pr create --head "$BRANCH" --base "${{ inputs.base }}" --title "$title" --body "$body")"
          fi
          echo "pr_url=$pr_url" >> "$GITHUB_OUTPUT"
          echo "PR: $pr_url"

      - name: Dispatch CI explicitly
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH: ${{ steps.plan.outputs.branch }}
        run: |
          gh api -X POST "repos/${{ github.repository }}/actions/workflows/ci.yml/dispatches" -f ref="$BRANCH"
          echo "Dispatched CI on $BRANCH"
